import { dialogMotionClass } from './motion-style';
/** Shared geometry for editing and confirmation dialogs. */
export const dialogSurfaceClass = dialogMotionClass + ' ' + 'gap-[var(--dialog-section-gap)] p-[var(--dialog-inset)] [border-radius:var(--ui-radius-xl)] border border-[var(--ui-border-inset)] bg-popover text-popover-foreground [box-shadow:var(--ui-shadow-modal)]';
