# 项目修改规则

所有修改默认遵循 `docs/ui-system.md`，间距细节参见 `docs/spacing-system.md`。用户当前要求优先；常规修改不需要再次询问是否采用规范。

## 唯一来源

- 字体使用 `app/ui-tokens.css` 中的系统字体；字号、字重、间距、阴影、动效和层级消费公共 token。
- 项目与标签颜色只使用 `lib/colors.ts` 的六色 palette 与 normalizeColor。
- 同功能文案引用 `lib/ui-copy.ts`；新通用文案先加入词典，再由入口引用。
- 弹窗使用公共 DialogContent/AlertDialog；编辑弹窗选择 editorLayout，正文复用 DialogBody，字段与按钮复用公共组件。
- 菜单、分段切换、工具栏、拖动反馈优先使用已有公共组件和 recipe。
- 导航意图与默认状态遵循 `lib/navigation-policy.ts`；保留筛选、折叠、失败重试与保存状态的既有约定。

## 修改方式

先找到当前公共来源并直接修改，不追加旧值的覆盖，不复制一套同功能组件。删除被替换的旧样式、失效入口和无用引用。

专用业务布局、日期坐标和拖动范围可保留专用规则。缺少公共能力时先扩展现有组件；确实独立的能力才建立新规则并记录适用范围，不为凑统一破坏功能。

## 自动校验

`npm run dev`、`npm run lint`、`npm run build` 自动先执行 UI 校验，违规时退出；开发中的源文件变化自动校验并在终端提示。

交付前运行 `npm run check`（UI 规则、lint、类型和测试）。涉及渲染或布局再运行 `npm run build` 并检查相关实际页面；不要声称静态检查能保证视觉美观。

不得为通过校验删除、弱化或绕过规则。规范变更须同时更新公共来源、文档和相关校验；必要例外应有明确业务理由与范围。
