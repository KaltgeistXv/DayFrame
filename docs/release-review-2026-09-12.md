# DayFrame 上线前页面检查 · 2026-09-12

本轮保留现有页面结构、已确认的交互和原有工作区数据。没有执行部署或导入、清空数据。

## 已完成

- 统一重复控件、筛选面板和列表悬停区域的语义圆角；补充 spacing-system.md。
- 修复移除顶部栏后首页仍扣除旧高度的问题。
- 修复浏览器缩放和小数滚动位置导致日历工具栏日期偏移的问题。
- 修复备份导入遗漏首页布局偏好；校验布局中的尺寸和结构。
- 启动页面遵循已保存的默认视图；避免旧请求错误覆盖新请求结果。
- 未来和未排期任务的打卡入口显示“打卡记录”，避免误示为“今日打卡”。
- 调整日历滚动节点、媒体查询和日期选择器的状态同步方式。
- 补齐工作区接口和存储记录类型，清理无用状态、导入与不必要的宽泛类型。
- 修正嵌套 main、控件分组、状态提示等组件语义。需要保留的键盘操作及用户主动打开弹窗后的自动聚焦使用局部、带说明的检查例外。

## 验证结果

| 检查 | 结果 |
|---|---|
| TypeScript：npx tsc --noEmit --incremental false | 通过 |
| 代码检查：npm run lint | 通过 |
| 测试：node --test tests/*.test.mjs | 124 项通过 |
| 生产构建：npm run build | 通过 |
| 补丁空白检查：git diff --check | 通过 |

新增回归覆盖备份首页布局、小数与缩放滚动边界、未来及未排期任务打卡文案。

使用页面现有的 240 条演示任务核对首页、列表、看板、日/周/月/年历、甘特图、热力图、筛选展开及新建弹窗。浏览器检查期间未发现控制台错误或警告。桌面约 1439 CSS px 与窄屏约 727 CSS px 下，已检查的工作区没有页面级横向溢出；窄屏弹窗保持在视口内。日期选中态宽高一致，跨周、跨月及回到今天的日期显示正常。临时视口已恢复。

## 已知限制

生产构建仍提示 workspace 客户端分块超过 500 KiB（约 573 KiB，gzip 约 180 KiB）。构建成功，后续可按视图拆分加载以优化首次加载体积。

这次是本地页面及代码检查，不包含线上部署验收、真实网络下的性能测量或所有设备的交互覆盖；未对用户记录进行试写。

## 后续清理与首页调整

- 将有效样式合并到 globals.css，移除旧主题文件与导入。清除 333 个无引用选择器和 371 条被覆盖声明，CSS 源码约减少 15%。
- 删除受版本控制的 TypeScript 增量缓存，并加入忽略规则。同步预览样式检查，移除废弃界面断言。
- 清理后类型检查、Lint、124 项测试、生产构建及实际预览 CSS 检查通过。同尺寸下列表与月历采样样式一致；首页、看板仅存在点击位置造成的导航悬停状态差异。
- 按用户新要求将首页四个内容卡片统一高度：桌面 360px，窄屏 320px。浏览器确认四卡等宽等高、内部滚动正常；727 CSS px 宽度无页面横向溢出。
