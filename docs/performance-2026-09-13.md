# 性能调整（2026-09-13）

- 日历、项目甘特图、任务甘特图与热力图使用独立按需加载入口，加载提示限制在视图内容内。首次进入会加载对应模块，后续切换复用模块缓存。
- 连续日期数组只在窗口起点或列数变化时重建。任务甘特图缓存分组及允许排序的任务集合，任务行使用 React.memo；可见日期标题更新无需重建全部任务行。数据、日期窗口或操作属性变化仍正常更新。
- 保留既有筛选缓存、仅渲染当前视图和随可视宽度调整日期缓冲的行为。

## 验证

- node --test tests/*.test.mjs：125 项通过。
- TypeScript、oxlint、生产构建、git diff --check 通过。
- 本地 240 条演示任务：任务甘特图打开与下一时段、日历和热力图按需加载通过。
- 构建前存在大于 500 kB 的代码块警告，构建后无此警告。新工作台代码块 196.9 KiB，日历块 44.6 KiB（未 gzip）。拆包不等于等比例缩短加载时间；未提供网络受限条件下的首屏时间测量。
